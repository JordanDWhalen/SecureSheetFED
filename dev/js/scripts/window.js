$(window).on('load', function() {
  headerScrolled();
  $(window).scrollTop();
});

$(window).on('scroll', function() {
  headerScrolled();
  $(window).scrollTop();
});

$(window).on('resize', function() {

});
